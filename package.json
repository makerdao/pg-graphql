{
  "name": "dai-pg",
  "version": "0.0.1",
  "private": true,
  "author": "dc",
  "license": "GPL-3.0",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "bignumber.js": "^6.0.0",
    "dotenv": "^5.0.0",
    "pg-promise": "^7.4.1",
    "web3": "^1.0.0-beta.29"
  },
  "scripts": {
    "clean": "rm -rf dist && mkdir dist",
    "build": "babel -d dist/ bin/sync.js lib/*.js libexec/*",
    "sync": "node dist/bin/sync.js",
    "start": "babel-node bin/sync.js",
    "postinstall": "npm run build"
  }
}
